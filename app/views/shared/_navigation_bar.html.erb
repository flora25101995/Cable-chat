<nav class="navbar navbar-expand-lg navbar-dark background-dark justify-content-between">
  <% if has_role?(:admin) %>
    <a class="navbar-brand retext-warning" href="#">Admin</a>
  <% else %>
    <a class="navbar-brand retext-warning" href="#">User</a>
  <% end %>
  <%
=begin%>
 <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-bar-collapse" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button> 
<%
=end%>

  <% if current_user %>
    <div class="dropdown">
      <a class="nav-link dropdown-toggle retext-warning" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <img class="avatar" src="<%= current_user.gravatar_url %>">
        <%= current_user.name %>
      </a>

      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
      </div>
    </div>
  <% end %>
</nav>
